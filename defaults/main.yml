# file: meta/main.yml

# Valgrind
name: valgrind
version: 3.16.1
valgrind_url: https://sourceware.org/pub/{{name}}/{{name}}-{{version}}.tar.bz2
valgrind_tmp: "/tmp/{{name}}-{{version}}.tar.bz2"
valgrind_build: "/tmp/{{name}}-{{version}}"
build_dir: "/tmp/{{name}}-{{version}}-build"
valgrind_install_dir: "/opt/{{name}}-{{version}}"
