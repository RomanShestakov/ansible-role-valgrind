# file: meta/main.yml

# Valgrind
name: valgrind
version: 3.16.1
url: https://sourceware.org/pub/{{name}}/{{name}}-{{version}}.tar.bz2
tmp: "/tmp/{{name}}-{{version}}.tar.gz"
build: "/tmp/{{name}}-{{version}}"
build_dir: "/tmp/{{name}}-{{version}}-build"
install_dir: "/opt/{{name}}-{{version}}"
